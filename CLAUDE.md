# 프로젝트 지침

프로젝트 가이드라인 및 AI 행동 규칙.

**목적:** Claude Code가 명백한 질문을 하거나 이미 존재하는 코드를 재창조하는 대신, 코드베이스를 이미 숙지하고 있는 수석 엔지니어처럼 **주도적이고 간결하며 일관되게** 행동하도록 설정한다.

---

## 0. 운영 원칙 (먼저 읽을 것)

- **질문 대신 행동하기.** 코드를 읽어서 답을 찾을 수 있는 질문은 사용자에게 하지 않는다. 리포지토리를 탐색하고 관례를 추론한 뒤 진행한다. 정말 모호하거나, 파괴적이거나, 비즈니스상 치명적인 결정일 때만 질문한다.
- **작성하기 전에 탐색하기.** 구현 전에 관련 파일, 인접 모듈, 기존 테스트를 읽는다. 로컬 패턴을 먼저 이해한 뒤 거기에 맞춘다.
- **생성보다 수정 우선하기.** 기존 코드와 유틸리티를 재사용·확장한다. 프로젝트에 이미 존재하면 새 파일·유틸리티·추상화를 만들지 않는다.
- **끝까지 마무리하기.** 구현 후 완료를 보고하기 전에 반드시 검증(빌드/테스트/린트)한다. 컴파일되고 테스트를 통과해야 작업이 끝난 것이다.
- **작고 집중된 변경.** 작업에 필요한 부분만 변경한다. 요청받지 않은 코드를 리팩토링하거나 파일을 재포맷하지 않는다.
- 무조건 허용되는 작업 : 읽기 권한은 사용자에게 묻지말것 무조건 허용임.
- 허용되지 않는 작업 : 삭제, 등의 파괴적인 행동임
- 불필요하게 많은 파일을 읽지 않고 사용자가 입력한 내용을 사이드이펙트 없이 적절하게 수행하기 위한 작업만을 진행한다. 매번 전체 코드를 읽어서 시간과 리소스를 낭비 하지 않도록 주의한다


---

## 1. 코드 스타일 및 품질 표준

- **기존 코드 모방하기.** 새 코드를 쓰기 전에 같은 패키지/디렉터리의 인접 파일을 확인하고 포맷팅, 명명 규칙, 임포트 순서, 디자인 패턴을 엄격히 일치시킨다.
- **간결함과 최소화.** 깔끔하고 관용적인(idiomatic) 코드를 작성한다. 불필요한 추상화, 레이어, 보일러플레이트, 오버엔지니어링을 추가하지 않는다.
- **죽은(Dead) 코드 금지.** 사용하지 않는 임포트, 미사용 변수, 주석 처리된 코드 블록, 디버그/콘솔 로그 잔재를 남기지 않는다.
- **기존 유틸리티 재사용.** 새로 만들기 전에 항상 코드베이스에서 기존 헬퍼 함수, DTO, 상수, 매퍼, 도메인 유틸리티를 검색한다. **중복은 결함이다.**
- **자기 설명적 코드.** 주석보다 명확한 이름을 우선한다. 주석은 '무엇(what)'이 아닌 **'왜(why)'** — 드러나지 않는 의도, 엣지 케이스, 트레이드오프 — 를 설명할 때만 단다.
- **기존 의존성 세트 준수.** 정말 필요한 경우가 아니면 새 라이브러리/의존성을 추가하지 않는다. 추가해야만 한다면 명시적으로 알리고 기존 옵션으로 해결할 수 없는 이유를 설명한다.

---

## 2. 유지보수성 및 일관성

- **개인 선호보다 일관성.** 코드베이스와 "모범 사례(Best Practice)"가 충돌하면 코드베이스를 따른다. 부분적으로 "더 나은" 불일치 스타일보다 일관된 스타일 하나가 훨씬 낫다.
- **단일 책임 원칙.** 함수와 클래스는 한 가지 역할에 집중시킨다. 줄 수를 줄이려고가 아니라 가독성이나 재사용성이 실질적으로 좋아질 때만 분리/추출한다.
- **영리함보다 가독성.** 다음에 이 코드를 읽을 사람을 위해 최적화한다. 의도를 가리는 영리한 한 줄짜리 코드는 피한다.
- **명시적 에러 처리.** 에러를 의미 있게 처리하고 예외를 조용히 삼키지 않는다. 프로젝트의 기존 에러/예외 처리 전략을 따른다.
- **에러 응답 최소화 (대원칙).** 클라이언트에 내려보내는 것은 **HTTP 상태코드 + 사용자에게 보여줄 한 문장**뿐이다. 그 이상은 프론트엔드/클라이언트가 알 필요가 없다.
  - 예외 원문(`ex.message`)을 그대로 응답 body 에 싣지 않는다. 응답 문구는 **핸들러가 직접 쓴 문장**이어야 한다.
  - 스택트레이스, 내부 클래스·패키지 경로, SQL·쿼리 조각, 상류 시스템 응답 원문, 내부 PK/식별자는 **서버 로그에만** 남긴다.
  - 원인 추적은 로그(+ 필요하면 상관관계 ID)로 한다. 화면은 "무엇이 잘못됐고 무엇을 하면 되는지"만 말한다.
- **과도하게 방어적인 코드 금지.** 주변 코드와 타입 시스템이 보장하는 경우 불필요한 널 체크, `try/catch`, 유효성 검사를 추가하지 않는다. 코드베이스가 이미 의존하는 계약(contract)을 신뢰한다.
- **네이밍 규칙.** 모듈의 용어(도메인 용어, `*Service`, `*Repository`, `*Dto`, `useX`, `withX` 같은 기존 접미사)와 일관되며 의도를 드러내는 이름을 쓴다.
- **공개 인터페이스(Public Surface) 최소화.** 외부 노출이 필요한 경우가 아니면 가장 좁은 범위/가시성(`private`/`internal`/모듈 로컬)을 쓴다.

---

## 3. 구현 규칙

- 명시적으로 요구되지 않는 한 과도하게 방어적인 코드를 쓰지 않는다.
- 뻔한 로직에 장황한 주석을 달지 않는다.
- 코드베이스가 불변성을 지키는 부분에서는 이를 준수한다(`val`, `const`, `readonly`, 불변 컬렉션 등). 공유 상태를 직접 변경(mutate)하지 않는다.
- 아래 언어별 모범 사례를 준수한다.
- 가능한 경우 함수를 순수(pure)하게 유지하고 사이드 이펙트를 두지 않는다.
- 편집할 때, 기존 공개 API 변경이 작업 목적이 아니라면 이를 보존한다.

### Java / Kotlin

- 기존 스타일(Google/JetBrains 스타일, 탭 vs 공백, 임포트 순서)에 맞춘다.
- `var`보다 `val`을 선호하고, 기본적으로 불변 컬렉션을 사용한다.
- 생성자 주입(Constructor Injection)을 사용하고 기존 DI 패턴(Spring 등)을 따른다.
- 기존 DTO/Entity 매핑 방식을 사용한다. 새로 직접 만들지 않는다.
- Kotlin 파일이면 Kotlin 관용구(`data class`, `sealed`, 스코프 함수, 널 안전성)를, Java 파일이면 Java 모범 사례(`Optional`, `record`, Stream)를 쓴다.
- 논-널(non-null) 계약이 있는 곳에 널 가능성을 도입하지 않는다.

### JavaScript / TypeScript

- `tsconfig`의 엄격함을 준수한다. 타입 지정을 우회하려고 `any`를 쓰지 말고 올바른 타입을 추론하거나 선언한다. 새 타입을 정의하기 전에 기존 타입/인터페이스를 재사용한다.
- 이미 사용 중인 모듈 시스템(ESM vs CommonJS)을 따른다.
- 프로젝트의 기존 비동기 스타일(`async/await` vs Promise)을 따른다.
- `const`를 선호하며, 코드가 불변성을 쓰는 곳에서는 `readonly`/불변성을 적용한다.
- 기존 프레임워크 패턴(React 훅 규칙, 컴포넌트 구조, 상태 관리)을 정확히 따른다. 새로운 패턴을 도입하지 않는다.
- 커밋되는 코드에 `console.log`를 남기지 않는다.

---

## 4. 워크플로우 및 검증

코드를 생성하거나 변경한 후, 완료를 선언하기 전에 다음을 검증한다.

1. 주변 파일과의 포맷팅/린트 일관성
2. 타입 체크(TS) / 컴파일(Kotlin·Java)
3. 아래 명령어를 사용한 빌드 및 테스트
4. 변경 내용과 검증 결과에 대한 짧은 요약 보고

### 빌드 및 테스트 명령어

**백엔드 — `hamster-back/` (Gradle 멀티모듈, Kotlin)**

> ⚠️ **JDK 21 고정.** 이 머신의 기본 JDK 는 25 이고, Gradle 8.14.3 은 25(class file major 69)를 못 읽어
> `Unsupported class file major version 69` 로 **즉시 실패**한다. 모든 gradle 명령 앞에 JAVA_HOME 을 붙인다.
>
> ```bash
> export JAVA_HOME=$(/usr/libexec/java_home -v 21)
> ```

| 목적 | 명령어 (`hamster-back/` 에서 실행) |
| --- | --- |
| 컴파일 (가장 빠른 검증) | `./gradlew :hamster-api:compileKotlin` |
| 빌드 | `./gradlew :hamster-api:build` |
| 테스트 | `./gradlew test` |
| 단일 테스트 | `./gradlew test --tests "com.hamsterapi.MyTest"` |
| 실행 (local 프로파일) | `./gradlew :hamster-api:bootRun --args='--spring.profiles.active=local'` |

- 모듈: `hamster-api`(실행 앱) · `hamster-batch` · `library-core` · `place-core` · `iam-core` · `foundation`
- 린트/포맷 태스크(spotless, ktlint)는 **설정되어 있지 않다.** 있는 것처럼 호출하지 않는다.

**프론트엔드 — `hamster-front/` (npm)**

| 목적 | 명령어 (`hamster-front/` 에서 실행) |
| --- | --- |
| 패키지 설치 | `npm ci` |
| 빌드 (타입체크 포함) | `npm run build` — `tsc -b && vite build` |
| 타입 체크만 | `./node_modules/.bin/tsc --noEmit` |
| 린트 | `npm run lint` |
| 개발 서버 | `npm run dev` → http://127.0.0.1:5173 |

- **`npx tsc` 를 쓰지 않는다.** `npx` 는 레지스트리의 무관한 `tsc` 패키지를 받아온다. 반드시 로컬 바이너리 경로로 호출한다.
- `test` / `typecheck` 스크립트는 **없다.** 타입 검증은 위의 `tsc --noEmit` 로 한다.

> 위 명령어 중 존재하지 않는 것이 있으면 `package.json` 스크립트나 `build.gradle` 태스크를 확인해 프로젝트의 실제 명령어를 사용한다. **명령어를 임의로 지어내지 않는다.**

---

## 5. 커뮤니케이션 스타일

- 간결하게 쓴다. 과정이 아닌 **결과부터** 전달한다.
- 미사여구, 요청 재확인/복창, 뻔한 단계 설명은 하지 않는다.
- 사소하지 않은 결정을 내렸으면 한 줄로 명시하고 넘어간다.
- 리스크, 블로커, 가정은 짧게 제시한다. 길게 늘어놓지 않는다.

**`.md` 문서 작성 한정 — So What / Why So 프레임워크.** 마크다운 문서(설계서, RFC, 리드미, 회고 등)를 쓸 때는 사실 나열이 아니라 So What / Why So 구조로 쓴다. **So What**: 모아둔 사실·데이터에서 "그래서 결론이 뭔가"를 뽑아 문단·섹션의 맨 앞에 결론으로 배치한다. **Why So**: 그 결론이 왜 성립하는지를 바로 아래에 근거(코드, 측정치, 제약, 트레이드오프)로 받쳐준다. 상위 결론 → 하위 근거가 위아래로 검증되도록 쓰고, 근거 없는 주장이나 결론 없는 사실 나열은 남기지 않는다.

- 주로 docs/ 디렉토리 하위에 문서를 남기며 모든 문서는 앞에 8자리 날짜 정보를 기입한다 (ex: 20260806, 20220707) 해당날짜는 문서를 개정한 날에 맞추어 변경한다

---

## 6. 하드 "절대 금지(Do NOT)" 리스트

- ❌ 코드를 읽어서 답을 찾을 수 있는 질문을 하지 않는다.
- ❌ 동등한 유틸리티/파일이 이미 있을 때 새로 만들지 않는다.
- ❌ 프로젝트에 없던 의존성, 프레임워크, 패턴을 추가하지 않는다.
- ❌ 사용하지 않는 임포트, 디버그 로그를 남기지 않는다.
- ❌ 작업과 관련 없는 코드를 재포맷하거나 리팩토링하지 않는다.
- ❌ 빌드/테스트를 거치지 않고 작업을 "완료" 처리하지 않는다.
- ❌ 혹시 몰라서 작성하는 투기적(speculative) 추상화를 만들지 않는다.

### 절대금지는 아니지만 되도록 피해야하나 고민해볼 여지가 있는것
- 죽은코드 : 의도적으로 죽은 코드를 남긴경우를 고려해서, 매 작업마다 죽은코드는 최대한 제거해야 하나, 사용자가 코멘트나 프롬프트를 통해서 명시적으로 살려둘것을 명령한 죽은 코드는 유지해야한다

---

## 7. 프로젝트 전용 컨텍스트

사내 도서관 · 공간(회의실) 예약 · 이슈(지라형) 관리를 묶은 풀스택 사내 업무 플랫폼.

### 스택

- **백엔드:** Kotlin 2.2 · Spring Boot 4.1 · JDK 21 · **WebFlux + R2DBC (리액티브 / 코루틴)** · Flyway · springdoc(OpenAPI) · Gradle(Groovy DSL, 멀티모듈)
- **프론트엔드:** React 19 · TypeScript · Vite · Tailwind CSS v4 · React Router v7 · axios · ag-grid · recharts · npm
- **DB:** PostgreSQL (DB명 `library`, `public` 스키마)
- **배포:** Docker + nginx (`infra/docker-compose.yml`)

> **리액티브가 기본이다.** 블로킹 코드(JPA, `RestTemplate`, `Thread.sleep`, 블로킹 JDBC)를 요청 경로에 넣지 않는다.
> 리포지토리는 `CoroutineCrudRepository`, HTTP 클라이언트는 `WebClient`, 컨트롤러 메서드는 `suspend` 를 쓴다.
> 예외: **Flyway 는 JDBC 로만 동작**하므로 마이그레이션용 커넥션(`custom.flyway.*`)은 R2DBC 와 별개로 유지한다.

### 모듈 구조

```
hamster-back/          # Gradle 멀티모듈
├── foundation/        #   공유 커널 (식별자·값객체·페이지네이션). 스프링 의존 없음
├── iam-core/          #   계정/사용자 도메인 골격
├── library-core/      #   도서관 도메인 + 유스케이스 계약(포트) + 서비스
├── place-core/        #   공간 예약 도메인
├── hamster-api/       # ★ 유일한 실행 앱: 컨트롤러 · R2DBC 어댑터 · auth/ · config/
└── hamster-batch/     #   배치 잡

hamster-front/src/
├── app/               # 레이아웃(Header/GNB) · MDI 탭 시스템
├── pages/             # library/ · place/ · jira/ · auth/ · playground/
├── core/              # auth(AuthContext/RequireAuth) · http(axios+refresh) · types
└── components/        # 공용 UI
```

- 레이어: `api(컨트롤러/DTO) → app(포트/페이로드) → infra(persistance, jwt)` — **포트·어댑터**. 도메인 코어는 스프링/DB 를 모른다.
- 새 기능은 **수직 슬라이스**로 추가한다. 자세한 절차는 `documents/추가-개발-가이드.md`.

### 인증 · 권한 (가장 자주 건드리는 영역)

```
브라우저 → GET /api/v0/auth/login → (302) IDP → /callback
        → 계정 JIT 프로비저닝 → access(JWT, 메모리) + refresh(HttpOnly 쿠키)
        → 프론트가 POST /api/v0/auth/refresh 로 access 부트스트랩
        → 업무 API 는 Authorization: Bearer, 401 시 /refresh 자동 재발급(single-flight)
```

- **IDP 독립.** 인증 코어는 외부 IDP 를 모른다. IDP 교체 = `IdentityProvider` 어댑터 추가 + `auth.idp` 설정 변경뿐 (`MockIdentityProvider` / `SsoIdentityProvider`, 선택은 `IdpConfig`).
- **access token 은 메모리에만.** `localStorage` 에 토큰을 넣지 않는다(XSS 표면). refresh 는 JS 가 못 읽는 HttpOnly 쿠키(`path=/api/v0/auth`).
- **권한은 2단인데 보안 경계는 백엔드 하나뿐이다.** 프론트의 메뉴/라우트 게이팅(`RequireAuth`, `canAccessNav`)은 UX 일 뿐이고, 실제 강제는 백엔드 `AdminAuthorization.verify()`(403)다. **프론트 게이팅만 걸고 끝내지 않는다.**
- 역할: `USER` / `MANAGER` / `SUPER_ADMIN`. `SUPER_ADMIN` 은 모든 검사를 통과한다.
- refresh 는 불투명 랜덤값이고 DB 엔 SHA-256 해시만 저장한다. 회전(rotation) + 재사용 탐지(family 전체 폐기).

### 주요 도메인 용어

`Account`(사내 계정 원장, 기준 식별자 = **ldapId**) · `IdentityLink`(외부 IDP 신원 ↔ Account) · `ExternalIdentity`(IDP 가 준 신원) · `AuthInfo`(요청 시점 인증 주체, DB 미저장) · `IdP`(SSO/Mock) · `Loan`/`BookSku`/`Inventory`(도서관) · `Place`/`Building`/`Room`(공간)

### 설정 · 환경

- **프로파일:** `local`(개발자 PC) · `dev`(개발 VM, mock IDP) · `prod`(운영, SSO + HTTPS 쿠키). 미지정 시 `local`.
- 값은 `application.yml`(공통) → `application-{profile}.yml` 순으로 override 된다.
- **시크릿은 반드시 환경변수로 주입한다.** `AUTH_JWT_SECRET`, `SSO_CLIENT_SECRET`, DB 비밀번호를 레포에 커밋하지 않는다. 템플릿: `infra/.env.example`.
- **`auth.idp=mock` 은 개발 전용이다.** Mock IDP 는 아이디/비번 없이 `admin.demo`(자동 `SUPER_ADMIN`) 로 로그인시킨다. 운영 성격 환경 설정을 만들 때 mock 이 기본값으로 새어 들어가지 않는지 확인한다.
- DB 스키마 변경은 **Flyway 마이그레이션 파일 추가**로만 한다(`hamster-api/src/main/resources/db/migration/V{n}__*.sql`). 기존 마이그레이션 파일을 수정하지 않는다.

### 테스트

- 테스트 코드가 **아직 없다.** 테스트 프레임워크·관례가 정해지지 않았으므로, 요청 없이 임의로 테스트 스택을 도입하지 않는다.
- 따라서 4절의 검증은 실질적으로 **컴파일 + 타입체크**다. 그 둘은 반드시 통과시킨다.

### 수정하면 안 되는 것들

- `backup/` — 예전 프론트 스냅샷. 참조만 하고 손대지 않는다.
- `documents/` 의 기존 설계 문서(`1_요구사항`, `2_설계_*`, `3_구현_*`) — 요청 없이 고치지 않는다. 신규 문서는 5절대로 `docs/` 하위.
- 이미 적용된 Flyway 마이그레이션 파일.

---

## 8. 작업 마무리 시 커밋 메시지 가이드 (필수)

코드를 변경하는 작업을 마쳤으면, 마지막 응답에 **커밋 메시지 가이드**를 반드시 포함한다.

- 커밋 메시지는 커밋 대상 파일의 **실제 diff 내용 기준**으로 작성한다 (대화 맥락이 아니라 변경된 코드가 근거).
- 형식:

  ```
  type: 한 줄 요약 (한국어, 50자 이내)

  - 주요 변경사항 bullet (무엇을 왜 바꿨는지)
  - ...
  ```

- `type`: `feat` / `fix` / `refactor` / `docs` / `chore` / `ci` 등 conventional commit 스타일.
- 성격이 다른 변경이 섞여 있으면(예: 기능 + 배포설정) 커밋을 나누라고 제안하고, 각 커밋별 메시지를 따로 제시한다.
- 커밋 대상이 아닌 기존 작업트리 변경이 섞여 있으면 구분해서 알려준다.
